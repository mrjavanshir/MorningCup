# Deployed Cloudflare Worker that stores game state behind a short id.
# Leave unset to keep everything packed into the URL as base64 (the default).
# See worker/README.md for how to deploy it.
VITE_STORE_URL=
